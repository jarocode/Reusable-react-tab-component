import React, {useContext} from 'react';
import styled from '@emotion/styled';

const Header = () => {
    return ( 
        <h1>WELCOME TO MY REACT ANIMATION!!</h1>
     );
}
 
export default Header;